name: tracord
description: 迹录 · Tracord — 地图旅行规划应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  provider: ^6.1.0
  shared_preferences: ^2.3.0
  flutter_map: ^8.3.0
  flutter_map_cancellable_tile_provider: ^3.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/中国_市.geojson
